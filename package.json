{
  "name": "sc-landing-pages",
  "version": "1.0.0",
  "description": "SC Landing Pages - Full Stack App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "client:install": "cd client && npm install",
    "client:build": "cd client && npm run build",
    "setup": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^10.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.20.0"
  }
}
